/* query the HTML document for the button. */

/**
 * Calculates a tip and shows the tip amount and
 * total with tip amount. Also tells the user if
 * the input amounts are invalid.
 */
function calculateTip() {
	/* get the total amount and tip total amount.
	 * 
	 * if they are valid numbers, use them to
	 * calculate the tip total and total with tip
	 * included, and show them to the user.
	 * 
	 * if they are not valid numbers, let the user
	 * know. */
};

/* add the event listener to run calculateTip()
 * when the button is clicked. */